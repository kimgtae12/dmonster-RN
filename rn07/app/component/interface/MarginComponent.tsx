import React from 'react';
import {View, Text} from 'react-native'

interface MarginComponentType {
    mt? : number,
    mb? : number,
    my? : number,
}

export const MarginComponent = ({ //margin top & margin bottom 컴포넌트입니다.
    mt,
    mb,
    my,
}:MarginComponentType)=>{

}